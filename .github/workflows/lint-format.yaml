name: format and lint
on: [push]
jobs:
  lint-format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Lint
        run: npm run lint
      - name: Format
        run: npm run format
      - name: Unit test
        run: npm run test
