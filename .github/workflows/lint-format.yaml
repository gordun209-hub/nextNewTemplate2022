name: format and lint
on: [push]
jobs:
  lint-format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Lint
        run: yarn lint
      - name: Format
        run: yarn  format
      - name: Unit test
        run: yarn test
